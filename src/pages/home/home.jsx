import Calendar from "../../components/Calendar/Calendar";

const Home = () => {
  // const dispatch = useDispatch();
  // const handleLogout = () => {
  //   dispatch(startLogout());

  return (
    <>
      <Calendar />
    </>
  );
};

export default Home;
